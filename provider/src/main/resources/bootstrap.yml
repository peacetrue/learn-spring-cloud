eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8700/eureka

spring:
    application:
        name: provider
    cloud:
        config:
            label: master
            discovery:
                enabled: true
                serviceId: config-center

#logging:
#  level:
#    "org.springframework": trace
