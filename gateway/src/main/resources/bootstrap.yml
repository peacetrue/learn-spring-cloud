eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8700/eureka/

spring:
    application:
      name: gateway
    cloud:
      config:
        label: master
        discovery:
          enabled: true
          serviceId: config-center
